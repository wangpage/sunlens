# SunLens 产品优化方案（对标 NotebookLM）

> 视角：产品经理 · 日期：2026-06-30
> 一句话定位：**NotebookLM 把"你喂的文档"变成知识产物；SunLens 把"你的屏幕操作"自动变成可复用的 SOP 与培训材料。**

---

## 1. NotebookLM 这个界面有什么功能

| 区域 | 功能 | 本质 |
|---|---|---|
| 左栏 **来源** | 添加 PDF/网站/文本/视频/音频/云盘；「网络搜索新来源 + Fast Research」自动找资料 | 用户自选的**知识底座（grounding 语料）** |
| 中栏 **对话** | 只基于来源问答，回答**带引用**、不臆造；可加笔记 | 接地问答（grounded chat with citations） |
| 右栏 **Studio** | 一键生成：音频概览/视频概览/演示文稿/思维导图/报告/闪卡/测验/信息图/数据表格 | 把来源**转成多种产物** |

**范式**：`来源（接地）→ 带引用问答 → 一键多产物`。一切都锚定用户自己的来源。

---

## 2. SunLens 与 NotebookLM 的关系（同范式，不同领域）

NotebookLM 的"来源"要**人工喂**；SunLens 的"来源"是**自动观察你操作生成**的——这是我们的护城河。

| 维度 | NotebookLM | SunLens（目标形态） |
|---|---|---|
| 来源从哪来 | 人工导入文档 | **自动录制你操作目标窗口（fnOS NAS 等）** |
| 一个"来源" | 一篇文档 | **一次操作会话**（帧 + 动作 + 输入事件） |
| 问答 | "这篇论文讲了啥" | **"上次在 NAS 上怎么建共享文件夹？"** 带帧/时间戳引用 |
| Studio 产物 | 学习资料 | **操作 SOP / 工作流图 / 培训测验 / 动作表格** |
| 数据出不出网 | 上云 | **全本地（Ollama），画面不出本机** |

> 杀手级用法：**你做一遍，它产出 SOP 和培训材料**。NotebookLM 做不到"自动记录操作过程"——这正是 SunLens 该 All-in 的差异点。

---

## 3. 现状盘点（精简移植后）

✅ 已具备：目标窗口检测 → 实时抓帧（事件驱动 + aHash 去重）→ 本地 qwen3-vl 逐帧理解 → SQLite 落库 → 三栏仪表盘（会话/时间轴/关键词问答）。

⚠️ 已知缺口/缺陷（实测得出）：
1. **抓帧遮挡**：`ImageGrab` 截的是屏幕矩形，目标窗口被遮挡时会截到错误内容（已复现）。
2. **理解太慢**：CPU 上 ~2–3 分钟/帧，一个 43 帧会话要跑 ~1 小时，**无法边录边看懂**。
3. **无产物层**：精简时移除了 Studio（NotebookLM 的灵魂），目前只能看时间轴 + 关键词检索。
4. **问答无接地引用**：仅 LIKE 关键词，未对齐 NotebookLM 的"带引用"体验。
5. **隐私**：NAS 页面的地址/账号/密码被原样截图存盘（本地不出网，但明文留存）。

---

## 4. 优化路线图（按优先级）

### P0 — 地基：先让"录得对、跑得起"
| # | 事项 | 解决的痛点 | 工作量 |
|---|---|---|---|
| P0-1 | **抓帧换 `PrintWindow`(ctypes GDI，带 `PW_RENDERFULLCONTENT`)** | 遮挡/后台也能抓到窗口真实内容；Chrome 网页可正确截取 | 小（仅改 `window_grabber.py`） |
| P0-2 | **关键帧采样**：一个会话只挑代表帧理解（去掉连续 `input`/`diff≈0` 冗余帧，按"画面显著变化 + 操作边界"取帧） | 把"理解一个会话"从 ~1 小时降到几分钟 | 中（`action_builder` 选帧逻辑） |
| P0-3 | **异步理解队列 + 边录边理解** | 不再"录完再 understand"，仪表盘实时填充 | 中 |

### P1 — NotebookLM 化：把"记录"变"知识"（核心价值）
| # | 事项 | 说明 | 工作量 |
|---|---|---|---|
| P1-1 | **恢复 Studio（本地 Ollama 驱动）**，先做三张高价值卡：**报告/SOP、思维导图、数据表格** | 原项目已有实现，纯文本产物本地模型能跑；SOP 是杀手产物 | 中 |
| P1-2 | **来源化多选**：一次会话=一个"来源"，可多选会话跨会话生成 | 对齐 NotebookLM 来源面板交互 | 小 |
| P1-3 | **接地问答 + 引用**：回答附带帧缩略图/时间戳跳转 | 对齐 NotebookLM citation 体验 | 中 |
| P1-4 | **本地脱敏**：正则(手机号/邮箱/IP/卡号) + 关键词(密码/token) 命中即打码，**存盘前**生效（Windows 友好，不依赖 Apple Vision） | 解决凭据明文留存 | 小 |

### P2 — 体验与规模
| # | 事项 | 说明 |
|---|---|---|
| P2-1 | **语义记忆回归**：用 Ollama 本地 embedding 替代原 DashScope 向量，问答从关键词升级为语义检索 |
| P2-2 | **音频/视频概览**：视频概览=帧+解说幻灯片（纯前端可做）；音频概览用 Windows SAPI / edge-tts 本地合成 |
| P2-3 | **性能与部署**：GPU 部署指引（理解提速 10×+）、可选更小更快模型、帧存储压缩与按会话清理 |

---

## 5. 关键产品决策（建议）

1. **聚焦"操作→SOP"这一条价值链**，而不是全面复刻 NotebookLM 九宫格。先把 **报告/SOP** 做到惊艳，它最能体现"你做一遍我帮你沉淀"的独特价值。
2. **性能是产品前提**：CPU 下务必走"关键帧采样 + 异步"，否则"实时记录"体验不成立；正式场景建议 GPU。
3. **隐私作为卖点**：强调"全本地、画面不出本机"，再叠加存盘前脱敏，面向 NAS/运维/内网场景就是差异化卖点。

## 6. 下一步（建议本周顺序）
1. P0-1 抓帧换 PrintWindow（修对"录得对"）→ 立刻 `peek` 验证
2. P0-2 关键帧采样（修对"跑得起"）
3. P1-1 恢复 Studio 的 **报告/SOP** 卡（拿出第一个"知识产物"）

> 这三步做完，SunLens 就从"会录屏会看懂"升级为"**你操作一遍，它给你一份 NAS 操作 SOP**"——这才是对标 NotebookLM 的真正形态。

---

# 里程碑记录（2026-06-30 实施）

P0/P1 地基已落地（PrintWindow 抓帧、关键帧采样、SOP 卡片），并在真实 NAS 会话上跑通"录→理解→SOP"闭环。随后围绕一条核心——**理解的"深度"**——做了三级跃迁，是本项目最关键的认知升级。

## 一、理解深度的三级跃迁：工具层 → 意图层 → 内容层

产品的灵魂是"理解得有多深"。我们经历了三层：

| 层级 | 它回答的问题 | 典型输出（同一段 NAS 看视频的会话） |
|---|---|---|
| **L1 工具层**（最初） | 屏幕上**有什么**、点了**哪里** | "在飞牛 fnOS 用内置播放器预览一个 mp4 文件" |
| **L2 意图层**（中途） | 用户在完成**什么任务、为什么、想要什么信息** | "用户在审查录屏，想验证文件能否播放 / 影视登录是否正常" |
| **L3 内容层**（最终） | 用户**消费的内容是什么**、看它**到底为了获取什么** | "视频是一段 **Aesthesis 设计理念演示**（讲皮肤感知/神经响应/美学与功能融合）；用户在**回顾学习该理念的内涵与实践，用于创作参考/知识整理**" |

**两条关键认知（均由产品负责人当场指出，倒逼升级）：**
1. **L1→L2**："记录不该是逐帧字面描述，要理解用户的**行为与意图**、想获取什么信息。" → 催生了**会话级意图分析**。
2. **L2→L3**："看视频/读文档这类**信息消费**行为，目的在**所看内容**里，不在'用播放器'这个工具上。" → 催生了**操作型 vs 信息消费型**的区分：对信息消费，必须读出**内容主题 + 用户的信息目标**。

> 这是 SunLens 与"屏幕字幕工具"的本质分野：它理解的是**人在追求什么信息**，而非屏幕上有什么像素。

## 二、支撑这套理解的机制（已实现并验证）

- **会话级意图分析**（`engine/intent.py`）：整段动作流 + 输入线索 → `task / why / info_sought / key_steps / outcome`，仪表盘单独成「🎯 意图摘要」块。
- **反思自检（reflection）**：先出草稿，再带证据自我批判（是否过度推断/停在工具层/漏了内容目标）并修订，输出 `confidence + reflection`。实测中**草稿一度答空/停在工具层，反思把它救回并下沉到内容层**。
- **反馈记忆（不改权重的"越用越准"）**：用户一句纠正存库，下次分析作为上下文喂回（in-context learning）。实测中模型在反思里**主动引用"既往反馈要求"并照做**——验证了无需微调即可持续改进。
- **逐帧叙事上下文**：每帧理解带最近 5 步，不再孤立描述；信息消费帧会读出所看内容主题。

> 重要边界（诚实记录）：浏览器**后台标签页**抓不到（只渲染活动标签）；视频内容靠**关键帧逐帧读屏**还原，非真正"看完整段视频"；"测试播放 vs 消费内容"的歧义由模型显式点出或由用户纠正消解。

## 三、模型与性能：32B 升级 + 上下文调优（双赢）

- **换模型**：`qwen3-vl:8b-instruct` → `qwen3-vl:32b-instruct`。质量显著提升：读出完整文件名、分清子模块（文件管理/影视/播放器）、**读懂视频里在放什么**、意图推断更准更接地。
- **真正的性能杠杆不是 CPU/GPU，而是上下文**：日志证实模型层早已 100% 在 **Radeon 8060S（ROCm）** 上；拖慢的是 Ollama 给 qwen3-vl 的**默认 262K 上下文**撑爆 KV 缓存（32B 涨到 101GB、53% 溢出回 CPU）。
- **解法**（`Modelfile.32b`）：`PARAMETER num_ctx 8192`，建 `sunlens-vl:32b`。footprint **101GB→21GB**、**53%CPU→100%GPU**、单帧 **2–3 分钟→45 秒**。**32B 比原来的 8B 还快，质量却高一档。**

| 指标 | 8B(262K) | 32B(262K) | **sunlens-vl:32b(8K)** |
|---|---|---|---|
| 占用 | ~CPU 溢出 | 101 GB | **21 GB** |
| 处理器 | 慢 | 53% CPU | **100% GPU** |
| 单帧 | 2–3 min | 更慢 | **~45 s** |
| 质量 | 糊 | 高 | **高** |

## 四、沉淀的方法论（给后续迭代）

1. **产品的护城河是"理解深度"**：持续追问"再深一层是什么"——工具→意图→内容→（下一步）跨会话的知识关联。
2. **反思 + 反馈 > 调参**：推理时改不动权重；用**自我批判**纠当下、用**反馈记忆**纠长期，更轻、可审计、越用越准。
3. **性能先看上下文再谈换硬件/模型**：超大默认上下文是本地大模型最隐蔽的性能陷阱。
4. **每一步用真实数据验证**：本里程碑所有结论都来自同一段真实 NAS 会话的端到端实跑。
